<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.feicui.edu.newsapp">

    <!-- <instrumentation -->
    <!-- android:name="android.test.InstrumentationTestRunner" -->
    <!-- android:targetPackage="com.feicui.edu.newsapp"/> -->
    <application
        android:name=".NewsAppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <uses-library android:name="android.test.runner" />

        <activity android:name=".demo.AysnProgressActivity">

        </activity>
        <activity android:name=".activity.NewsWebActivity" />
        <activity android:name=".activity.LeadActivity">
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity android:name=".activity.LogoActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".demo.MyFragmentActivity" />
        <activity android:name=".demo.FragmentViewPagerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>